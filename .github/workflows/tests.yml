name: testsecho.echo on:echo   push:echo   pull_request:echo.echo jobs:echo   test:echo     runs-on: ubuntu-latestecho.echo     steps:echo       - uses: actions/checkout@v4echo.echo       - uses: actions/setup-python@v5echo         with:echo           python-version: "3.11"echo.echo       - name: Install dependenciesecho         run: echo           python -m pip install --upgrade pip && pip install -r requirements.txt && pip install pytestecho.echo       - name: Run testsecho         run: pytest -q
